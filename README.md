How did you dynamically create and append new elements to the DOM?
I used JavaScript methods like createElement and appendChild to add elements to the page instead of writing everything in the HTML. This made the form more interactive and allowed updates without reloading the page.

What steps did you take to ensure accurate updates to the total price?
For this assignment I focused more on making sure the form inputs were checked correctly. I made sure the validation runs every time the form is submitted so the information is always up to date.

How did you handle invalid input for product name or price?
I added checks to make sure the fields were not empty and that the password met the minimum length. If something was invalid, I displayed an error message under the input.

What challenges did you face when implementing the remove functionality?
One challenge was making sure the correct messages showed at the right time and cleared properly when the user fixed the input.
